// Shared categorical colors for disease-page data visualizations —
// same fixed hues already used on The Need hub's Bronx charts, so a
// reader sees one consistent color language across every chart on
// the site rather than a new palette per page. Text never carries
// these colors — only fills/swatches do.
export const viz = {
  blue: "#2a78d6",
  orange: "#eb6834",
  green: "#1baf7a",
  amber: "#eda100",
  pink: "#e87ba4",
  purple: "#4a3aa7",
  neutral: "#9a8f6f",
};
